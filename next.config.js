/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com','i.postimg.cc','plus.unsplash.com'],
  },
}

module.exports = nextConfig
